---
interface Props { class?: string }
const { class: className = '', ...attributes } = Astro.props;
---
<article {...attributes} class:list={['rounded-lg border border-slate-200 bg-white p-6 shadow-sm', className]}><slot /></article>
