---
interface Props { title?: string; class?: string }
const { title, class: className = '' } = Astro.props;
---
<section class:list={['mx-auto max-w-6xl px-5 py-10 sm:py-14', className]}>{title && <h2 class="mb-6 font-serif text-3xl font-bold text-navy-900">{title}</h2>}<slot /></section>
